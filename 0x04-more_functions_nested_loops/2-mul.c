#include "main.h"
/**
 * mul - a function that multiplies two integers.
 * @a : to be multiplied integres.
 * @b : to be multiplied integres.
 * Return: Always result (success).
 */
int mul(int a, int b)
{
	int result = a * b;

return (result);
}

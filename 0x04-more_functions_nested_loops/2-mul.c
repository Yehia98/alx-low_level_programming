#include "main.h"
/**
 * mul - multiplies two integers.
 * @a : to be multiplied integres.
 * @b : to be multiplied integres.
 * Return (result) for the value of multiplication.
 */
int mul(int a, int b)
{
	int result = a * b;

return (result);
}

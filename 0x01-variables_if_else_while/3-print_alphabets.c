#include <stdio.h>
/**
 * main - printing the alphabet.
 * Description: A program that prints the alphabet.
 * Return: Always 0 (success).
 */
int main(void)
{
	int a = 'a';
	int A = 'A';

	while (a <= 'z', A <= 'Z')
	{
		putchar(a);
		putchar(A);
		a++;
		A++;
	}
return (0);
}

#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @a : first number to be added.
 * @b : second number to be added.
 * Return: result.
 */
int add(int a, int b)
{
	int result;

	result = a + b;
return (result);
}
